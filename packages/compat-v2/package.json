{
  "name": "@discord-dashboard/compat-v2",
  "version": "3.0.0-alpha.0",
  "description": "Run v2 style settings on v3 without a rewrite.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"no lint yet\""
  },
  "dependencies": {
    "@discord-dashboard/core": "workspace:*",
    "@discord-dashboard/schema": "workspace:*"
  }
}
