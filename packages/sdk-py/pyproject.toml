[project]
name = "discord-dashboard"
version = "3.0.0a0"
description = "Bot adapter SDK for Python. Connects a bot to the dashboard gateway."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["websockets>=12.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
